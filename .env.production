# Committed on purpose: a public, non-secret base URL that every production
# build must have. See lib/assets.ts. A host-level variable of the same name
# overrides it.
NEXT_PUBLIC_ASSETS_URL=https://pub-58692fc1b63d4ff0b2011f9ad888bc28.r2.dev/site
